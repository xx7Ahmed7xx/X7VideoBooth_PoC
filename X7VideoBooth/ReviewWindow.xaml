<Window x:Class="X7VideoBooth.ReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:X7VideoBooth"
        mc:Ignorable="d"
        Closing="Window_Closing"
        Title="ReviewWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <MediaElement x:Name="mediaPlayer" Grid.Row="0" Stretch="Uniform" 
                      VerticalAlignment="Top" UnloadedBehavior="Manual"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
            <Button x:Name="keepBtn" Content="Keep" Width="80" Margin="10" 
                    Click="keepBtn_Click">
            </Button>
            <Button x:Name="againBtn" Content="Replay" Width="80" Margin="10" 
                    Click="againBtn_Click">
            </Button>
            <Button x:Name="reBtn" Content="Retake" Width="80" Margin="10" 
                    Click="reBtn_Click">
            </Button>
        </StackPanel>
    </Grid>
</Window>
